mos6502:
	- Add tests for:
		asl, bit, bcc, bcs, beq, bmi, bne, bpl, bvc, bvs, brk,
		dec, inc, lda, lsr, nop, rol, ror, sta

x86_64:
	- Add tests.

---------------------------------------------------

Full instruction-set implementation:
	- Add tests for all opcodes.
	- Document all opcodes.

