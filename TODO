mos6502:
	- Add tests for:
		asl, bit, bcc, bcs, beq, bmi, bne, bpl, bvc, bvs, brk, cpx, cpy,
		clc, cld, cli, clv, sec, sed, sei, dec, inc, lda, ldx, ldy,
		lsr, nop, rol, ror, sta, stx, sty
	- Document opcodes.

x86_64:
	- Add tests.

---------------------------------------------------

Full instruction-set implementation:
	- Add tests for all opcodes.
	- Document all opcodes.

