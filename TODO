mos6502:
	- Add tests for:
		bit, bcc, bcs, beq, bmi, bne, bpl, bvc, bvs, brk,
		dec, inc, lda, nop, sta

x86_64:
	- Add tests.

---------------------------------------------------

Full instruction-set implementation:
	- Add tests for all opcodes.
	- Document all opcodes.

